
    @if (User.Identity.IsAuthenticated)
    {
            <a class="nav-link " asp-area="" asp-controller="Bookings" asp-action="Index">My Bookings</a>
            <a class="nav-link" asp-controller="Bookings" asp-action="Index">
                Welcome @User.Identity.Name
            </a>
            <form class="form-inline" asp-controller="Account" asp-action="Logout"
              asp-route-returnUrl=@Url.Action("Index", "Account")>
                <button type="submit" class="nav-link btn btn-link">Logout</button>
            </form>
        
    }
    else
    {       
            <a class="nav-link" asp-controller="Account" asp-action="Login">Login</a>
            <a class="nav-link " asp-controller="Customers" asp-action="Create">Register</a>
    }